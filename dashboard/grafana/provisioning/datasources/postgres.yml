apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: postgres-ds
    type: postgres
    url: postgres:5432
    user: secadmin
    secureJsonData:
      password: secpass123
    jsonData:
      database: securitydb
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 2
      connMaxLifetime: 14400
      postgresVersion: 1600
      timescaledb: false
    editable: true
    isDefault: true
